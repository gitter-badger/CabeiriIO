import{GraphElement} from "./graph.elements/graph.element";

export class Graph
{
    //Variables/functions local to this graph. Persistent between execution of the graph
    public graph : Array<GraphElement>;
    constructor (){}
}
